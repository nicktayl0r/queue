extends ../layout

block content
    div.row 
        div.col-lg-8.text-editor
        div.col-sm-4.starter(data-starter=challenge.starterCode, data-cid=challenge._id)
            h1 #{challenge.title}
            p !{challenge.prompt}
            button.btn.btn-primary.submit-button Submit!

    script(src="/js/lib/codemirror.js")
    script(src="/js/lib/javascript.js")
    script
        include ./../scripts/codeMirrorLoader.js

    

//- Page where students will see a text editor and a prompt and will have to submit a challenge